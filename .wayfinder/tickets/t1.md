## Question

前后端接口契约如何定义：Tauri Commands 的参数与返回值、统一错误结构、任务状态机、事件格式（scan.progress / sync.progress / sync.completed / recovery.required），以及契约在前端的 TypeScript 表达。

## 依据

需求 §10.4（命令表与错误模型）、§7.2（计划失效）、§11.1（实体）。产出 `docs/api-contract.md`（权威文档）与 `src/lib/backend-contract.ts`（前端类型），由后端会话维护。
