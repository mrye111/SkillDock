## Question

文件监听与变更提醒后端如何实现：watcher 服务订阅源根（递归）、1.5 秒密集事件合并、连续两次相隔 500ms 状态一致后才触发重扫、启动/睡眠恢复/监听溢出后完整校验、扫描结果经 scan.progress 推送变更提醒；监听只触发重扫，不作为内容一致依据。

## 依据

需求 §7.5 前三段。测试：临时目录注入文件事件，断言合并窗口、稳定复核与溢出全量路径。
