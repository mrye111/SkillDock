## Question

自动同步引擎如何实现 §7.5 全量保护语义：默认关闭；开启时明确源库/目标/允许范围且默认仅已授权映射；新发现技能先提醒不自动分发；仅执行无冲突新增/更新（含托管内正常文件增删）；整项移除、非托管接管、目标漂移、规则变化大范围删除、权限变化均转待处理；技能总数归零或源根不可读时停止执行不生成删除计划；全局暂停开关。

## 依据

需求 §7.5。复用计划器/执行器；自动路径的计划生成与人工路径的差异（跳过冲突而非等待处理）需明确建模与测试。
