## Question

契约 v1.5 汇总与前后端联调：watcher.progress / auto_sync.status / tray 状态桥接等事件格式，方案/标注/导入导出命令的最终形态，docs/api-contract.md 与 src/lib/backend-contract.ts 同步升版，与前端会话对齐后冻结。

## 依据

契约维护规则（接口变化先文档后实现并通知前端）。被监听、自动同步、方案、标注、导入导出五票阻塞。
