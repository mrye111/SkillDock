## Question

本地备注、收藏与用户标签如何实现：以「库 ID + 相对路径」为键的标注表（备注文本、收藏标记、标签集合），随技能列表返回与编辑；搜索扩展为按名称、描述、目录和标签匹配；数据独立于技能文件，不写源目录。

## 依据

需求 §3.2 P1 第四条、§7.1 列表字段约束。契约扩展：SkillView 增加 annotations，新增 get/set_annotation 命令与标签筛选。
