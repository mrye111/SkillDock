## Question

深色主题与命令面板如何实现（前端主导）：深色主题含「跟随系统」选项，与浅色同为有意设计（需求 §9.1 视觉体系扩展）；命令面板（Ctrl+K 类入口）覆盖主要动作（切换库、预览同步、重试失败项、打开日志目录等）；两者均不破坏现有信息架构与可达性。

## 依据

需求 §3.2 P1 末条、§9.1（P1 增加暗色及跟随系统）。由前端会话认领执行，后端无需改动（主题状态持久化走后端 settings 表，契约 v1.5 顺带包含）。
