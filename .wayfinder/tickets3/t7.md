## Question

设置与映射导入导出如何实现：导出库/目标/映射/忽略/备份设置为 JSON（不含快照与绝对路径授权，§11.2）；导入时重新确认并绑定路径（库路径、目标路径逐条核对或重选），完成后停留在预览不立即执行；导出格式带版本号，向后兼容策略明确。

## 依据

需求 §3.2 P1 第五条、§11.2 末段。契约扩展：export_config / import_config 命令与导入预览模型。
